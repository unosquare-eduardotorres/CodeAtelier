// This hook has been removed in favor of explicit typed methods on window.api.
// See src/preload/index.ts for the available API methods.
// Use window.api.methodName() directly instead of invoke(channel, args).
