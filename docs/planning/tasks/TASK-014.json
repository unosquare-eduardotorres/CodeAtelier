{
  "id": "TASK-014",
  "title": "Create quality gate service",
  "description": "Create src/main/services/quality-gate.service.ts. Detect project type from workspace (package.json, tsconfig.json, etc). Run applicable gates: tsc --noEmit, eslint, npm test, electron-vite build. Return structured GateResult array.",
  "feature_ref": "F002",
  "task_type": "backend",
  "complexity": {
    "score": 7,
    "factors": {
      "files_affected": 3,
      "estimated_lines": 250,
      "new_dependencies": 0,
      "risk_flags": ["external_integration"]
    }
  },
  "dependencies": ["TASK-002"],
  "acceptance_criteria": [
    "Detects TypeScript, ESLint, Jest from project files",
    "Runs tsc --noEmit with proper tsconfig",
    "Runs eslint with project config",
    "Runs npm test with --passWithNoTests",
    "Runs electron-vite build for full verification",
    "Each gate returns passed/failed with output",
    "Gate timeout configurable (default 2 min)"
  ],
  "suggested_agent": "backend:api-developer-typescript"
}
