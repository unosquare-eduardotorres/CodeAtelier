{
  "id": "TASK-024",
  "title": "Wire task artifacts into specialist pool",
  "description": "Before spawning each specialist, gather outputs from dependency tasks via taskArtifactService.readPriorOutputs(). Build enriched prompt with prior context. After completion, save output via writeTaskOutput().",
  "feature_ref": "F004",
  "task_type": "backend",
  "complexity": { "score": 5, "factors": { "files_affected": 2, "estimated_lines": 80, "new_dependencies": 0, "risk_flags": [] } },
  "dependencies": ["TASK-023", "TASK-016"],
  "acceptance_criteria": [
    "Prior task outputs included in specialist prompt",
    "Specialist output saved as structured markdown",
    "State file updated after each task completion",
    "Artifact paths recorded in state.json"
  ],
  "suggested_agent": "backend:api-developer-typescript"
}
