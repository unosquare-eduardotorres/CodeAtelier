{
  "all": true,
  "src": ["src"],
  "_comment": "Coverage is scoped to the main process + shared code, which is what the custom unit suite (src/main/__tests__/run-all.ts) exercises. The renderer (React .tsx) is covered by e2e/manual testing, not these unit tests, so including it here only diluted the line ratio and made test:cov:check ungateable.",
  "include": [
    "src/main/**/*.ts",
    "src/shared/**/*.ts"
  ],
  "exclude": [
    "**/__tests__/**",
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/*.d.ts",
    "src/**/index.ts",
    "src/main/db/migrations/**",
    "src/shared/types.ts",
    "src/shared/mpa-types.ts",
    "src/main/services/agent-session.types.ts",
    "src/main/services/executor-types.ts",
    "e2e/**",
    "out/**",
    "dist/**"
  ],
  "reporter": ["text", "text-summary", "html", "lcov"],
  "report-dir": "./coverage",
  "exclude-after-remap": true,
  "skip-full": false,
  "clean": true
}
