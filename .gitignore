node_modules
dist
out
.DS_Store
.eslintcache
*.log*
*.tgz
.claude/worktrees/
e2e/screenshots/
e2e/test-results/
.repomap.tags.cache/
description-cache.db
chroma/
.agentstudio/

# Agent Studio worktrees
.agent-studio/

# Coverage reports (c8)
coverage/

# Native binding backup (build-mac.sh temp dir)
.native-binding-backup/

# Process manager state (auto-generated, workspace-specific)
.pm-state/

# Build lock (build-mac.sh / build-win.sh concurrency guard)
.build-lock/

# TypeScript incremental build state (emitted by tsc -p tsconfig.*.json)
*.tsbuildinfo

# Scratch screenshots from live E2E sessions (never commit)
.tmp-screens/

# Ad-hoc scratch screenshots at repo root (live debugging sessions)
.tmp-shot*.png
.tmp-shot*.jpg
.tmp-screenshot.png

# Emergency DB recovery backups taken during live incident triage
.blueprint-*-recovery-backup-*.sql
